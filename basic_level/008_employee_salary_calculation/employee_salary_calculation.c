#include <stdio.h>

// TODO: The following include is unnecessary and can be removed
#include <stdlib.h> // This line can be removed as 'abort' is not used anymore

/*
 * Implement a function that calculates the salary of an employee.
 *  - employee_number: The employee's number (1 ≤ employee_number).
 *  - hours_worked: The number of hours worked (1 ≤ hours_worked).
 *  - hourly_rate: The hourly wage (0 ≤ hourly_rate).
 *
 * Output:
 * NUMBER = 1
 * SALARY = $ 4100.00
 *
 * OBS: Use only a single printf to output the result.
 */
void employee_salary_calculation(unsigned int employee_number, unsigned int hours_worked, float hourly_rate)
{

  // TODO: Remove this line and implement the addition function correctly
  fprintf(stderr, "ERROR: Function 'employee_salary_calculation' is not implemented yet!\n");
  abort(); // Immediately terminates the program
}
